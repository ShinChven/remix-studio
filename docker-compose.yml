services:
  # app:
  #   build: .
  #   image: shinchven/remix-studio
  #   container_name: remix-studio-app
  #   ports:
  #     - "${APP_PORT:-3000}:3000"
  #   environment:
  #     # Network endpoints hardcoded for Docker internal networking
  #     DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-remix_studio}
  #     MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://minio:9000}
      
  #     # Secrets and configs pulled from .env file implicitly by Docker Compose
  #     MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
  #     MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
  #     MINIO_BUCKET: ${MINIO_BUCKET:-remix-studio}
  #     MINIO_EXPORT_BUCKET: ${MINIO_EXPORT_BUCKET:-remix-studio-exports}
      
  #     DEFAULT_ADMIN_EMAIL: ${DEFAULT_ADMIN_EMAIL:-admin@example.com}
  #     DEFAULT_ADMIN_PASSWORD: ${DEFAULT_ADMIN_PASSWORD:-your_secure_password}
  #     PROVIDER_ENCRYPTION_KEY: ${PROVIDER_ENCRYPTION_KEY}
  #     JWT_SECRET: ${JWT_SECRET}
      
  #     NODE_ENV: production
  #     PORT: 3000
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     minio:
  #       condition: service_started

  postgres:
    image: postgres:17-alpine
    container_name: remix-studio-postgres
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-remix_studio}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: remix-studio-minio
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_DRIVE_MAX_USAGE_PCT: 99
    volumes:
      - ./data/minio:/data
    command: server /data --console-address ":9001"
